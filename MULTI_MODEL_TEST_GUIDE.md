# 多模型功能测试指南

## 测试场景

### 场景1：开启多模型模式
1. 点击聊天界面左下角的"多模型 (关闭)"按钮
2. **预期结果**：自动打开多选模型选择器弹窗
3. 选择2个模型（例如：gpt-4o 和 claude-3.5-sonnet）
4. 点击"确认选择 (2)"按钮
5. **预期结果**：
   - 弹窗关闭
   - 多模型按钮变为"多模型 (开启) 2个"
   - 右下角Token计数器显示"2 模型"

### 场景2：选择最多4个模型
1. 点击多模型按钮打开选择器
2. 依次选择4个不同的模型
3. **预期结果**：标题显示"选择多个模型 (4/4 个已选择)"
4. 尝试选择第5个模型
5. **预期结果**：
   - 弹出提示："最多只能选择4个模型"
   - 未选中的模型显示为灰色禁用状态
   - 鼠标悬停显示提示

### 场景3：取消选择（少于2个模型）
1. 点击多模型按钮打开选择器
2. 只选择1个模型
3. 点击"X"关闭选择器
4. **预期结果**：
   - 选择器关闭
   - 多模型模式自动关闭
   - 多模型按钮恢复为"多模型 (关闭)"

### 场景4：查看多模型Token统计
1. 开启多模型模式并选择3个模型
2. 发送一条消息，等待所有模型回复
3. 鼠标悬停在右下角Token计数器上
4. **预期结果**：
   - 显示横向排列的3个模型的Token统计
   - 每个模型显示：
     - 模型名称
     - Token使用量（例如：1.2K/128K）
     - 使用率百分比（例如：0.9%）
     - 进度条（颜色根据使用率变化）

### 场景5：关闭多模型模式
1. 在多模型模式开启状态下
2. 点击多模型按钮
3. **预期结果**：
   - 多模型模式关闭
   - 按钮变为"多模型 (关闭)"
   - Token计数器恢复单模型显示
   - 弹出提示："多模型模式已关闭"

### 场景6：多模型并发回复
1. 开启多模型模式，选择3个模型
2. 发送一条消息
3. **预期结果**：
   - 3个模型同时开始回复（并发请求）
   - 每个模型的回复独立显示
   - 每个模型显示自己的名称和提供商

## 边界测试

### 测试1：只选择1个模型
- 操作：在选择器中只选择1个模型，点击确认
- **预期结果**：确认按钮禁用，显示"(至少选择2个模型)"

### 测试2：不选择任何模型
- 操作：打开选择器，不选择任何模型，点击确认
- **预期结果**：确认按钮禁用，无法点击

### 测试3：选择后清空
- 操作：选择3个模型，点击"清空选择"按钮
- **预期结果**：所有选择被清空，确认按钮禁用

### 测试4：移动端显示
- 操作：在移动端设备或窄屏幕下测试
- **预期结果**：
   - Token统计纵向排列
   - 选择器适配小屏幕
   - 所有功能正常工作

## 性能测试

### 测试1：4个模型同时回复
- 操作：选择4个模型，发送较长的消息
- **观察**：
   - 响应速度
   - 流式更新是否流畅
   - 是否有卡顿或延迟

### 测试2：长对话Token统计
- 操作：进行多轮对话（10轮以上）
- **观察**：
   - Token统计是否准确
   - 悬停显示是否流畅
   - 内存使用是否正常

## 兼容性测试

### 浏览器
- [ ] Chrome
- [ ] Firefox
- [ ] Safari
- [ ] Edge

### 设备
- [ ] 桌面端（1920x1080）
- [ ] 平板端（768x1024）
- [ ] 移动端（375x667）

## 已知问题

1. **多模型消息横向排列**：当前多模型回复仍然是纵向排列，横向排列功能需要进一步开发
2. **其他语言本地化**：除中文和英文外，其他语言文件需要添加"Models"字段

## 测试通过标准

- ✅ 所有场景测试通过
- ✅ 边界测试无异常
- ✅ 性能测试流畅
- ✅ 兼容性测试通过
- ✅ 无控制台错误
