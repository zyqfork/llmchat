# 多模型横向排列功能演示

## 功能概述

多模型模式现在支持横向排列显示，让你可以同时对比多个AI模型的回答，就像一个AI竞技场！

## 快速开始

### 1. 开启多模型模式

点击聊天界面左下角的 **"多模型 (关闭)"** 按钮

→ 自动打开模型选择器

### 2. 选择模型

在弹出的选择器中：
- 选择 2-4 个你想对比的模型
- 例如：GPT-4o、Claude-3.5-Sonnet、Gemini-2.0-Flash
- 点击 **"确认选择 (3)"** 按钮

### 3. 发送消息

在输入框中输入你的问题，例如：
```
请用简单的语言解释什么是量子计算
```

### 4. 查看横向排列的回复

发送后，你会看到：

```
┌─────────────────────────────────────────────────────────────────┐
│ 👤 你的问题：请用简单的语言解释什么是量子计算                      │
└─────────────────────────────────────────────────────────────────┘

┌────────────────────────┐  ┌────────────────────────┐  ┌────────────────────────┐
│ 🤖 gpt-4o @OpenAI      │  │ 🤖 claude-3.5          │  │ 🤖 gemini-2.0          │
│         [🔄] [📋]      │  │ @Anthropic  [🔄] [📋] │  │ @Google     [🔄] [📋] │
│                        │  │                        │  │                        │
│ 量子计算是一种         │  │ 量子计算利用...        │  │ 简单来说...            │
│ 利用量子力学...        │  │                        │  │                        │
│                        │  │                        │  │                        │
│ [正在生成...]          │  │ [正在生成...]          │  │ [正在生成...]          │
│                        │  │                        │  │                        │
│ 45.2 t/s  14:30        │  │ 38.7 t/s  14:30        │  │ 52.1 t/s  14:30        │
└────────────────────────┘  └────────────────────────┘  └────────────────────────┘
```

**注意**：操作按钮（🔄重试、📋复制）默认隐藏，鼠标悬停在卡片上时才显示

## 功能特点

### 🎯 并发回复
- 所有模型同时开始回复
- 无需等待，实时看到每个模型的进度

### 📊 直观对比
- 横向排列，一目了然
- 可以同时看到不同模型的回答风格
- 方便对比回答质量和速度

### 🎨 美观界面
- 卡片式设计，清晰区分
- 每个模型有独立的头像和标识
- 渐变背景，现代化UI

### ⚡ 性能指标
- 实时显示每个模型的回复速度（tokens/秒）
- 显示回复时间
- 方便评估模型性能

### 🔧 独立操作
- 每个模型的回复可以独立复制
- 可以单独重试某个模型的回答
- 操作按钮默认隐藏，鼠标悬停时显示
- 互不干扰

### 📱 响应式设计
- 桌面端：横向排列，最多4个模型
- 移动端：自动调整卡片宽度
- 支持横向滚动

## 使用技巧

### 💡 选择合适的模型组合

**对比不同厂商**：
- GPT-4o (OpenAI)
- Claude-3.5-Sonnet (Anthropic)
- Gemini-2.0-Flash (Google)

**对比同厂商不同版本**：
- GPT-4o
- GPT-4o-mini
- GPT-3.5-turbo

**对比不同特性**：
- 推理模型：o1-preview
- 快速模型：gpt-4o-mini
- 平衡模型：claude-3.5-sonnet

### 🎯 适用场景

1. **创意写作**：对比不同模型的创意和风格
2. **技术问答**：看哪个模型的解释更清晰
3. **代码生成**：对比代码质量和效率
4. **翻译对比**：看哪个模型翻译更准确
5. **总结归纳**：对比总结的完整性

### 📊 查看Token统计

鼠标悬停在右下角的Token计数器上：

```
┌──────────────────────────────────────────────────────────────┐
│ ┌─────────┐  ┌─────────┐  ┌─────────┐                       │
│ │gpt-4o   │  │claude-3 │  │gemini-2 │                       │
│ │1.2K/128K│  │800/200K │  │1.5K/1M  │                       │
│ │0.9%     │  │0.4%     │  │0.15%    │                       │
│ │████░░░░ │  │██░░░░░░ │  │█░░░░░░░ │                       │
│ └─────────┘  └─────────┘  └─────────┘                       │
└──────────────────────────────────────────────────────────────┘
```

可以看到每个模型的：
- Token使用量
- 上下文大小
- 使用率百分比
- 可视化进度条

## 关闭多模型模式

再次点击 **"多模型 (开启)"** 按钮即可关闭

→ 恢复单模型模式

## 常见问题

### Q: 为什么只能选择最多4个模型？
A: 为了保证界面清晰和性能流畅，限制最多4个模型。这样可以在不横向滚动的情况下看到所有回复。

### Q: 可以在移动端使用吗？
A: 可以！移动端会自动调整卡片宽度，支持横向滚动查看所有模型。

### Q: 如何单独重试某个模型？
A: 鼠标悬停在该模型卡片上，会显示操作按钮，点击"重试"按钮即可，不会影响其他模型。

### Q: 为什么看不到操作按钮？
A: 操作按钮默认隐藏，鼠标悬停在卡片上时会自动显示。移动端会始终显示。

### Q: 多模型模式会消耗更多Token吗？
A: 是的，每个模型都会独立计费。建议在需要对比时使用，日常对话可以使用单模型模式。

### Q: 可以保存多模型的对话吗？
A: 可以！所有模型的回复都会保存在对话历史中。

## 快捷键

- `Ctrl/Cmd + Shift + C`：复制最后一条回复
- `Shift + Esc`：聚焦输入框
- `Ctrl/Cmd + /`：显示快捷键列表

## 反馈

如果你在使用过程中遇到任何问题或有改进建议，欢迎反馈！

---

**享受多模型对比的乐趣吧！🎉**
